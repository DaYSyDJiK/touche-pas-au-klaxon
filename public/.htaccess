RewriteEngine On
RewriteBase /

# Si le fichier ou le dossier demandé n'existe pas, on redirige vers index.php
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Tout est redirigé vers index.php
RewriteRule ^ index.php [QSA,L]
